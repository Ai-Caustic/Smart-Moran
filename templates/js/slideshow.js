function slider() {
  let slider = document.getElementById("slider1");
  let path = "smartmoran\\static\\Images\\SmartCurtainControl\\";
  let type = ".jpg";
  let images = [
    `${path}` + `smc1` + `${type}`,
    `${path}` + `smc2` + `${type}`,
    `${path}` + `smc3` + `${type}`,
  ];
  let imageNumber = Math.floor(Math.random() * images.length);
  slider.src = images[imageNumber];
}
function slider2() {
  let slider = document.getElementById("slider2");
  let path = "Images\\SmartWaterLeakageControl\\";
  let type = ".jpg";
  let images = [
    `${path}` + `swlc1` + `${type}`,
    `${path}` + `swlc2` + `${type}`,
    `${path}` + `swlc3` + `${type}`,
  ];
  let imageNumber = Math.floor(Math.random() * images.length);
  slider.src = images[imageNumber];
}
function slider3() {
  let slider = document.getElementById("slider3");
  let path = "Images\\SmartIntruderSystem\\";
  let type = ".jpg";
  let images = [
    `${path}` + `sis1` + `${type}`,
    `${path}` + `sis2` + `${type}`,
    `${path}` + `sis3` + `${type}`,
  ];
  let imageNumber = Math.floor(Math.random() * images.length);
  slider.src = images[imageNumber];
}
function slider4() {
  let slider = document.getElementById("slider4");
  let path = "Images\\SmartGateAutomation\\";
  let type = ".jpg";
  let images = [
    `${path}` + `sga1` + `${type}`,
    `${path}` + `sga2` + `${type}`,
    `${path}` + `sga3` + `${type}`,
  ];
  let imageNumeber = Math.floor(Math.random() * images.length);
  slider.src = images[imageNumeber];
}
function slider5() {
  let slider = document.getElementById("slider5");
  let path = "Images\\SmartCCTV\\";
  let type = ".jpg";
  let images = [
    `${path}` + `sc1` + `${type}`,
    `${path}` + `sc2` + `${type}`,
    `${path}` + `sc3` + `${type}`,
  ];
  let imageNumeber = Math.floor(Math.random() * images.length);
  slider.src = images[imageNumeber];
}
function slider6() {
  let slider = document.getElementById("slider6");
  let path = "Images\\WifiDoorSensorWindowSensor\\";
  let type = ".jpg";
  let images = [
    `${path}` + `wds1` + `${type}`,
    `${path}` + `wds2` + `${type}`,
    `${path}` + `wds3` + `${type}`,
  ];
  let imageNumeber = Math.floor(Math.random() * images.length);
  slider.src = images[imageNumeber];
}
function slider7() {
  let slider = document.getElementById("slider7");
  let path = "Images\\FireDetection\\";
  let type = ".jpg";
  let images = [
    `${path}` + `fd1` + `${type}`,
    `${path}` + `fd2` + `${type}`,
    `${path}` + `fd3` + `${type}`,
  ];
  let imageNumeber = Math.floor(Math.random() * images.length);
  slider.src = images[imageNumeber];
}
function slider8() {
  let slider = document.getElementById("slider8");
  let path = "Images\\SmartPowerSockets\\";
  let type = ".jpg";
  let images = [
    `${path}` + `sps1` + `${type}`,
    `${path}` + `sps2` + `${type}`,
    `${path}` + `sps3` + `${type}`,
  ];
  let imageNumeber = Math.floor(Math.random() * images.length);
  slider.src = images[imageNumeber];
}
function slider9() {
  let slider = document.getElementById("slider9");
  let path = "Images\\SmartSecurityLighting\\";
  let type = ".jpg";
  let images = [
    `${path}` + `ssl1` + `${type}`,
    `${path}` + `ssl2` + `${type}`,
    `${path}` + `ssl3` + `${type}`,
  ];
  let imageNumeber = Math.floor(Math.random() * images.length);
  slider.src = images[imageNumeber];
}
function slider10() {
  let slider = document.getElementById("slider10");
  let path = "Images\\SmartIndoorLighting\\";
  let type = ".jpg";
  let images = [
    `${path}` + `sil1` + `${type}`,
    `${path}` + `sil2` + `${type}`,
    `${path}` + `sil3` + `${type}`,
  ];
  let imageNumeber = Math.floor(Math.random() * images.length);
  slider.src = images[imageNumeber];
}
function slider11() {
  let slider = document.getElementById("slider11");
  let path = "Images\\SmartEnergyMonitoring\\";
  let type = ".jpg";
  let images = [
    `${path}` + `sem1` + `${type}`,
    `${path}` + `sem2` + `${type}`,
    `${path}` + `sem3` + `${type}`,
  ];
  let imageNumeber = Math.floor(Math.random() * images.length);
  slider.src = images[imageNumeber];
}
